# Python - Exceptions

This project covers Python exception handling using `try`/`except` blocks,
including safely accessing list elements without using built-in functions
like `len()`.

## Tasks

### 0. Safe list printing
Write a function that prints `x` elements of a list, using `try`/`except`
to safely handle out-of-range access, without using `len()` or any imports.
